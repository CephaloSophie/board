# server/.env — VPS AlmaLinux 217.160.186.250 · https://board.kantoaplo.com
# Généré par « npm run env:vps » (mot de passe MongoDB et JWT_SECRET aléatoires). chmod 600.
# PORT, HOST et CLIENT_ORIGIN sont aussi fixés par ecosystem.vps.config.cjs (prioritaires).

PORT=7002
HOST=127.0.0.1
MONGODB_URI=mongodb://board:CHANGER_MOT_DE_PASSE_MONGO@127.0.0.1:27017/boardKantoAplo?authSource=admin
JWT_SECRET=CHANGER_JWT_SECRET
JWT_EXPIRES_IN=30d
CLIENT_ORIGIN=https://board.kantoaplo.com
